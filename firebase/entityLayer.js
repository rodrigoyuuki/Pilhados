class noticias {
    constructor(
        content,
        createdAt,
        imageUri,
        summary,
        title,
    ) {
        this.content = content;
        this.createdAt = createdAt;
        this.imageUri = imageUri;
        this.summary = summary;
        this.title = title;
    }
}